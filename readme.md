# Laravel Vue ToDo App with User Login/Registration

## Setup

run ```composer install``` to install PHP libraries.

Change credentials of MySQL DB inside .env file.

run ```php artisan migrate``` to create schemas inside database

All done.